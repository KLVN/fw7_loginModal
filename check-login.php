<?php
if(($_POST['username']=='me') && ($_POST['password']=='1234'))
{
    echo "1";
}
?>

/* if the posted values (username + password) are the same as given here, then echo "1" what means "username and password are correct" */
